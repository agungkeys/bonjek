const CURRENT_ENV = `bonjek.id`;
const CURRENT_API = `https://bonjek-api.herokuapp.com`;
const CURRENT_HOST = `https://bonjek.akwebsite.xyz`;
const BASE_URL = CURRENT_HOST;

export default {
  CURRENT_ENV,
  CURRENT_API,
  CURRENT_HOST,
  BASE_URL,
}